{
  "name": "@agastyaone/db",
  "version": "0.1.0",
  "private": true,
  "description": "Schema, RLS policies, seeds and isolation tests for the AgastyaOne platform",
  "scripts": {
    "note": "Migrations are applied via the Supabase MCP or CLI; these scripts document the files, they do not connect on their own.",
    "list": "ls -1 migrations/*.sql",
    "test:isolation": "echo 'Run tests/rls_isolation.sql against the target project (transactional, rolls back).'",
    "check": "echo 'Run tests/schema_guards.sql against the target project; it raises on any violated invariant.'"
  }
}
