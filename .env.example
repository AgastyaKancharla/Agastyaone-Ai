# ---- Supabase (project: AgastyaOne Platform, ap-south-1) ----
NEXT_PUBLIC_SUPABASE_URL=https://xxjmtthbfqgxdapbxyng.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. NEVER expose to the browser and never import outside
# apps/worker or apps/platform/app/api/internal/** (enforced by ESLint).
SUPABASE_SERVICE_ROLE_KEY=

# ---- Worker ----
# Optional remote Playwright endpoint; omit to launch local Chromium.
PLAYWRIGHT_WS_ENDPOINT=
WORKER_CONCURRENCY=3
HEADLESS=true
